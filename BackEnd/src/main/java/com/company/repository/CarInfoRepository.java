package com.company.repository;

import com.company.entity.car.CarInfo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CarInfoRepository {
    // JpaRepository에서 제공하는 기본 메소드들 (save(), findById() 등)을 사용할 수 있습니다.
    // 추가적인 커스텀 쿼리 메소드가 필요한 경우 여기서 정의할 수 있습니다.

}
